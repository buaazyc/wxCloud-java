<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tencent.wxcloudrun.dao.GlowHistoryMapper">

    <!-- 结果映射 -->
    <resultMap id="GlowHistoryMapper" type="com.tencent.wxcloudrun.model.GlowHistory">
        <id property="msgId" column="msg_id"/>
        <result property="fromUserName" column="from_user_name"/>
        <result property="createTime" column="create_time"/>
        <result property="address" column="address"/>
        <result property="numQuality" column="num_quality"/>
        <result property="strQuality" column="str_quality"/>
    </resultMap>

    <!-- 插入访问记录 -->
    <insert id="insertGlowHistory" parameterType="com.tencent.wxcloudrun.model.GlowHistory">
        INSERT INTO t_glow_history ( msg_id, from_user_name, create_time, address, num_quality, str_quality )
        VALUES ( #{msgId}, #{fromUserName}, #{createTime}, #{address}, #{numQuality}, #{strQuality} )
    </insert>
</mapper>
